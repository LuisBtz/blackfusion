$( document ).ready(function() {

    $( "section.pre" ).delay(5000).fadeOut(1000);
    $( ".container" ).delay(6000).fadeIn(1500);
});

  document.addEventListener('DOMContentLoaded', function(){
      Typed.new('.element', {
        strings: ["Emprender su propio negocio", "Tener un horario flexible", "Obtener ingresos extras", "Ser su propio jefe", "Cumplir sus sueños"],
        typeSpeed: 0
      });
  });
